const MissLogin = () => {
  return (
    <div style={{ display: "flex", alignItems: "center", justifyContent: "center", minHeight: "100vh", backgroundColor: "#fff" }}>
      <p style={{ fontSize: "18px", color: "#333" }}>请插key，通过认证客户端登录</p>
    </div>
  );
};

export default MissLogin;
